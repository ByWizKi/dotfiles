{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build C++ file",
            "type": "shell",
            "command": "/opt/homebrew/bin/g++",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
